{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @inventory/contracts build && pnpm --filter @inventory/web build",
  "outputDirectory": "apps/web/.vercel/output"
}
