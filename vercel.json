{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @inventory/contracts build && pnpm --filter @inventory/web build && mv apps/web/.vercel/output .vercel/output"
}
